{
  "buildCommand": "cd ../backend && pip install -r requirements.txt",
  "devCommand": "cd ../backend && python main.py",
  "installCommand": "cd ../backend && pip install -r requirements.txt",
  "experimentalServices": {
    "frontend": {
      "routePrefix": "/",
      "framework": "nextjs"
    },
    "backend": {
      "entrypoint": "backend",
      "routePrefix": "/_/backend"
    }
  }
}
